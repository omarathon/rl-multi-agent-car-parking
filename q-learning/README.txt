Python 3.9.6 is required, with the packages within requirements.txt installed.

plot_fig5.1.sh plots Figure 5.1, plot_fig5.2.sh plots Figure 5.2 (when running on Windows, convert to a .sh script if not).

learn.slurm is an example .slurm file for training Q-Learning on the SLURM batch comptue system.

The other files are explained in the report.

